</ul>
<h1>Burgers</h1>

<h2>Choose a burger to devour or add a burger to devour later!</h2>

<ul>

    {{#each burgers}}

    {{#unless this.devoured}}
    <li>
        {{this.burger_name}}
        
        <div class="form-group">
            <input type="submit" class = "devoured" name="devoured" id="change-devoured" data-id="{{this.id}}" data-name="{{this.burger_name}}" value="devour"> Eat Me!
        </div>
    </li>
    {{/unless}}

    {{/each}}
</ul>

<h2>Burgers are amazing</h2>

<ul>
    {{#each burgers}}
    {{#if this.devoured}}
    {{this.burger_name}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="bur">Burgers</label>
        <input type="text" id="bur" name="name">
    </div>

    {{!-- <div class="form-group">
        <input type="submit" name="devoured" id="change-devoured" value="devour"> Eat Me!
    </div> --}}

    <button type="submit">Add Burger</button>
</form>